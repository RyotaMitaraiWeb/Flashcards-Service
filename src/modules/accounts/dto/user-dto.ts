/**
 * ```typescript
 * class UserDto {
    id: number;
    username: string;
}
 * ```
 * This DTO is used for account objects which will be used to generate JWTs
 */
export class UserDto {
  id: number;
  username: string;
}